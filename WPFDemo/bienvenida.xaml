<Window x:Class="WPFDemo.bienvenida"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemo"
        mc:Ignorable="d"
        Title="bienvenida" 
        Width="600"
        Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        
        >
    <Grid Background="#F3F4F6"  >

        <Border Width="500"
                Padding="40"
                Background="White"
                BorderBrush="#D1D5DB"
                BorderThickness="1"
                CornerRadius="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Ícono -->
                <TextBlock Grid.Row="0"
                           Text="👋"
                           FontSize="60"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"/>

                <!-- Título -->
                <TextBlock Grid.Row="1"
                           Text="¡Bienvenido!"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="#1F2937"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>

                <!-- Mensaje -->
                <TextBlock Grid.Row="2"
                           Text="Has iniciado sesión correctamente."
                           FontSize="17"
                           Foreground="#6B7280"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Botón -->
                <Button x:Name="btnCerrarSesion"
                        Grid.Row="3"
                        Content="Cerrar sesión"
                        Width="160"
                        Height="45"
                        FontSize="16"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        HorizontalAlignment="Center" Click="btnCerrarSesion_Click"/>

            </Grid>
        </Border>
    </Grid>
</Window>
